configuration:
  data_path:
    name: Data Path
    description: Path to your media library (e.g., /share/stash/data or /media/videos)
  log_level:
    name: Log Level
    description: The verbosity level for logging output
network:
  9999/tcp: Web interface
